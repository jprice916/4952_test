@page
@model ProcsOtherModel
@{
    ViewData["Title"] = "Processes using data binding";
}
<h1>@ViewData["Title"]</h1>

@foreach (var proc in Model.procs!)
{
    <div>
        <b>@proc.ProcessName</b> - ID: <a asp-page="/ProcDetails" asp-route-id="@proc.Id">@proc.Id</a>
    </div>
}
